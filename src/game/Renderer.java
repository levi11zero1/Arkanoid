package game;

import java.awt.Graphics2D;

/**
 * Minimal Renderer skeleton. Real rendering logic should be moved here from GamePanel.
 */
public class Renderer implements IRenderer {
    @Override
    public void render(Graphics2D g) { }
}
